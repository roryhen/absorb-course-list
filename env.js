import 'dotenv/config.js';

const absorbURL = process.env.ABSORB_URL;
const apiKey = process.env.ABSORB_API_KEY;
const username = process.env.ABSORB_USERNAME;
const password = process.env.ABSORB_PASSWORD;

export { absorbURL, apiKey, username, password }